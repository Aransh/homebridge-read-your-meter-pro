{
  "pluginAlias": "ReadYourMeterPro",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Exposes your Read Your Meter Pro water meter in HomeKit. HomeKit has no water-consumption service, so readings are surfaced as **light sensors** (the number is your consumption, the unit says \"lux\" — ignore it) and thresholds as **leak sensors** you can use for automations and notifications.",
  "footerDisplay": "For verbose logs, enable Homebridge debug mode (or the debug toggle on this plugin's child bridge). Unofficial; not affiliated with Arad Group or any water corporation.",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "Read Your Meter Pro",
        "required": true
      },
      "email": {
        "title": "Email",
        "type": "string",
        "format": "email",
        "required": true,
        "description": "The email address you use to sign in at rym-pro.com."
      },
      "password": {
        "title": "Password",
        "type": "string",
        "required": true,
        "x-schema-form": {
          "type": "password"
        }
      },
      "pollInterval": {
        "title": "Poll interval (minutes)",
        "type": "integer",
        "default": 60,
        "minimum": 15,
        "maximum": 1440,
        "description": "The meter itself only reports hourly at best, so there is nothing to gain below 60."
      },
      "unit": {
        "title": "Unit",
        "type": "string",
        "default": "liters",
        "oneOf": [
          {
            "title": "Litres",
            "enum": [
              "liters"
            ]
          },
          {
            "title": "Cubic metres (m³)",
            "enum": [
              "cubic_meters"
            ]
          }
        ],
        "description": "Litres read better in the Home app: 734 instead of 0.734."
      },
      "dailyThreshold": {
        "title": "Daily alert threshold",
        "type": "number",
        "default": 0,
        "minimum": 0,
        "description": "Trips the daily alert when today's consumption reaches this value, in the unit selected above. A rough starting point is 400 litres per person per day — roughly double a normal day, so it flags a genuine problem rather than a long shower. Watch the plugin's debug log for a week and adjust. 0 removes the sensor."
      },
      "monthlyThreshold": {
        "title": "Monthly alert threshold",
        "type": "number",
        "default": 0,
        "minimum": 0,
        "description": "Trips the monthly alert when this month's consumption reaches this value. 3500 litres per person per month is the subsidised household allocation in Israel, so that is a useful place to put it: the alert then means \"from here on the water costs about double\". 0 removes the sensor."
      },
      "exposeDaily": {
        "title": "Today's consumption",
        "type": "boolean",
        "default": true
      },
      "exposeMonthly": {
        "title": "This month's consumption",
        "type": "boolean",
        "default": true
      },
      "exposeForecast": {
        "title": "Month-end forecast",
        "type": "boolean",
        "default": true,
        "description": "The portal's own estimate of what this whole calendar month will add up to. Comparable to the monthly threshold."
      },
      "exposeTotal": {
        "title": "Cumulative meter reading",
        "type": "boolean",
        "default": false,
        "description": "Always reported in m³, since a lifetime total in litres exceeds HomeKit's 100000 ceiling."
      },
      "nameDaily": {
        "title": "Today's consumption",
        "type": "string",
        "placeholder": "Water Usage Today"
      },
      "nameMonthly": {
        "title": "This month's consumption",
        "type": "string",
        "placeholder": "Water Usage This Month"
      },
      "nameForecast": {
        "title": "Month-end forecast",
        "type": "string",
        "placeholder": "Water Monthly Forecast"
      },
      "nameTotal": {
        "title": "Cumulative meter reading",
        "type": "string",
        "placeholder": "Water Meter Total"
      },
      "nameDailyAlert": {
        "title": "Daily alert sensor",
        "type": "string",
        "placeholder": "Water Daily Alert"
      },
      "nameMonthlyAlert": {
        "title": "Monthly alert sensor",
        "type": "string",
        "placeholder": "Water Monthly Alert"
      }
    }
  },
  "layout": [
    "email",
    "password",
    {
      "type": "fieldset",
      "title": "Alerts",
      "expandable": true,
      "expanded": true,
      "items": [
        "dailyThreshold",
        "monthlyThreshold"
      ]
    },
    {
      "type": "fieldset",
      "title": "Sensors",
      "description": "Switching a sensor off removes it from HomeKit on the next restart. The two alert sensors are controlled by their thresholds above: 0 removes them.",
      "expandable": true,
      "expanded": false,
      "items": [
        "exposeDaily",
        "exposeMonthly",
        "exposeForecast",
        "exposeTotal"
      ]
    },
    {
      "type": "fieldset",
      "title": "Names",
      "description": "Leave these empty to name the sensors in the Home app instead — renames there are remembered across restarts. Filling one in pins the name, and a rename in the Home app will be undone on the next restart. HomeKit only accepts names that start and end with a letter or digit.",
      "expandable": true,
      "expanded": false,
      "items": [
        "nameDaily",
        "nameMonthly",
        "nameForecast",
        "nameTotal",
        "nameDailyAlert",
        "nameMonthlyAlert"
      ]
    },
    {
      "type": "fieldset",
      "title": "Advanced",
      "expandable": true,
      "expanded": false,
      "items": [
        "name",
        "unit",
        "pollInterval"
      ]
    }
  ]
}
